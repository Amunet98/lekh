# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# for Claude to read, not for the public repo — the repo is public, these are
# working notes. lekh-alpha.md was tracked by oversight and sat on GitHub for
# two days before being purged from history; the glob is here so a new note
# cannot repeat that by being created under a name nobody thought to add.
CLAUDE.md
lekh-*.md
