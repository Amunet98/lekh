# Lekh — standing rules

- Bump the patch version in `package.json` on every app change, however minor, then run `npm install` to sync `package-lock.json`. The footer (`src/components/Footer.tsx`) displays the version via `__APP_VERSION__`.
