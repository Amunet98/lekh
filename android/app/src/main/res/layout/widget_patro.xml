<?xml version="1.0" encoding="utf-8"?>
<!-- Deliberately plain. A widget is drawn through RemoteViews, which supports
     only a fixed set of views and cannot load a custom font, so the Devanagari
     renders in whatever the device provides for the script. Every Android
     device that ships a Nepali locale has one.

     The android:text values are placeholders: they are what the launcher's
     widget picker previews before the first render, so they show a real date
     rather than "TextView". -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="12dp"
    android:background="@drawable/widget_background">

    <TextView
        android:id="@+id/widget_day"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="38sp"
        android:textStyle="bold"
        android:includeFontPadding="false"
        android:textColor="@color/widget_text"
        android:text="२८" />

    <TextView
        android:id="@+id/widget_month"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="13sp"
        android:textColor="@color/widget_text"
        android:layout_marginTop="1dp"
        android:text="श्रावण २०८३" />

    <TextView
        android:id="@+id/widget_weekday"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:textColor="@color/widget_muted"
        android:text="बिहिबार" />

    <TextView
        android:id="@+id/widget_ad"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="10sp"
        android:layout_marginTop="3dp"
        android:textColor="@color/widget_muted"
        android:text="13 Aug 2026" />

    <!-- Hidden here: at 2x2 the festival line is the better use of the last
         row, and tithi already falls back into it when there is no festival. -->
    <TextView
        android:id="@+id/widget_tithi"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:text="" />

    <!-- The festival line. maxLines=2 with ellipsize because festival names run
         long (हरिशयनी एकादशी, तुलसी रोप्ने) and a widget cannot scroll. -->
    <TextView
        android:id="@+id/widget_note"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:gravity="center"
        android:maxLines="2"
        android:ellipsize="end"
        android:layout_marginTop="4dp"
        android:textColor="@color/widget_accent"
        android:text="औशी" />
</LinearLayout>
